    import XCTest
    @testable import DistrictFinder

    final class DistrictFinderTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DistrictFinder().text, "Hello, World!")
        }
    }
